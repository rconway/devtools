all:
  hosts:
  # vars:
  #   ansible_user: devtools
  children:
    gitlab:
      hosts:
      children:
        gitlab_masters:
          hosts:
            gitlab.rconway.uk:
              ansible_port: 8080
        gitlab_runners:
          hosts:
            gitlab-runner-[01:01].rconway.uk:
              ansible_port: 22
